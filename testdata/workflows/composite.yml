# Test: workflow using a composite action (triggers recursive resolution)
name: composite-test
on:
  push:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: nodeselector/actions-test-fixtures/simple-composite@main
      - run: echo "done"
